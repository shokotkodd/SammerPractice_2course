<Window x:Class="SummerPractice.StudentForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SummerPractice"
        mc:Ignorable="d"
        Title="Режим студента" Height="600" Width="600" WindowStartupLocation="CenterScreen">
    <Window.Background>
        <ImageBrush ImageSource="Images/1.jpg"/>
    </Window.Background>

    <Grid>

        <Menu  HorizontalAlignment="Center"  VerticalAlignment="Center"  FontFamily="Copperplate Gothic Bold" FontSize= "30"  Background="Transparent" Foreground="White">
            <MenuItem Header="Выбор темы" Background="Transparent" Foreground="White">
                <MenuItem Name="Topic1" IsCheckable="True" IsChecked="True" Header="Тема 1" Click="Topic_Click" ></MenuItem>
                <MenuItem Name="Topic2" IsCheckable="True" Header="Тема 2" Click="Topic_Click"></MenuItem>
                <MenuItem Name="Topic3" IsCheckable="True" Header="Тема 3" Click="Topic_Click"></MenuItem>
                <Separator></Separator>
                <MenuItem Name="Exit" Header="Выход" Click="Exit_Click"></MenuItem>
            </MenuItem>
            <MenuItem Name="Choose" Header="Выбор билета" Click="Choose_Click"></MenuItem>
        </Menu>

    </Grid>

</Window>
